(*
 * hyperglot fixture: .ml nests blocks and has no line comments.
 * [impl:ml/ml-block#1]
 *)

(* outer
  (* [impl:ml/ml-nested/deep#1] inner *)
  [impl:ml/ml-nested/after-close#1] still inside the outer block
*)
